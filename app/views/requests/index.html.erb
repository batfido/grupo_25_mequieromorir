<p id="notice"><%= notice %></p>

<h1>Solicitudes</h1>

<table>
  <thead>
    <tr>
      <th>Arrendatario</th>
      <th>Arrendador</th>
      <th>Articulo</th>
      <th>Cantidad</th>
      <th>Fecha de Devolución</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @requests.each do |request| %>
      <tr>
        <td><%= request.customer %></td>
        <td><%= request.offerer %></td>
        <td><%= request.item %></td>
        <td><%= request.quantity %></td>
        <td><%= request.due_date %></td>
        <td><%= link_to 'Ver Solicitud', request %></td>
        <td><%= link_to 'Editar Solicitud', edit_request_path(request) %></td>
        <td><%= button_to 'Eliminar Solicitud', request, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Request', new_request_path %>
